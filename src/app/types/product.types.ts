export type Discount = {
  amount: number;
  percentage: number;
};

export type Product ={
  id: number,
  title: string,
  image: string,
  description: string,
  price: string,
  category: string
}